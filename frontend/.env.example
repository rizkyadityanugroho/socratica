# Socratica frontend — public env vars
# Set this to your backend URL when deploying (empty = use relative /api/* paths)
# Examples:
#   PUBLIC_API_BASE_URL=                (dev: Astro proxy handles it)
#   PUBLIC_API_BASE_URL=https://api.socratica.app  (production)
PUBLIC_API_BASE_URL=
