{
  "name": "socratica-backend",
  "version": "1.0.0",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "dev": "concurrently -n be,fe -c blue,green \"node src/index.js\" \"npm run dev --prefix ../frontend\"",
    "test": "NODE_ENV=test vitest run",
    "test:watch": "NODE_ENV=test vitest",
    "test:coverage": "NODE_ENV=test vitest run --coverage",
    "start": "node src/index.js"
  },
  "dependencies": {
    "@google/genai": "^2.9.0",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "express-rate-limit": "^8.5.2"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^4.1.9",
    "concurrently": "^10.0.3",
    "supertest": "^7.2.2",
    "vitest": "^4.1.9"
  }
}
